upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name crstoreportal.foodconceptsplc.com www.crstoreportal.foodconceptsplc.com;
    charset utf-8;

    # Max upload size
    client_max_body_size 20M;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Static files
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Media files
    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # Let's Encrypt verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Django application
    location /api {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        
        # Timeouts
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Health check endpoint
    location /health/ {
        access_log off;
        proxy_pass http://django;
        proxy_set_header Host $host;
    }
}

# HTTPS configuration (uncomment after obtaining SSL certificates)
# server {
#     listen 443 ssl http2;
#     server_name crstoreportal.foodconceptsplc.com www.crstoreportal.foodconceptsplc.com;
#     charset utf-8;
#
#     # SSL certificates
#     ssl_certificate /etc/letsencrypt/live/crstoreportal.foodconceptsplc.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/crstoreportal.foodconceptsplc.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     client_max_body_size 20M;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /app/media/;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#         
#         proxy_connect_timeout 300s;
#         proxy_send_timeout 300s;
#         proxy_read_timeout 300s;
#     }
# }

# HTTP to HTTPS redirect (uncomment after obtaining SSL certificates)
# server {
#     listen 80;
#     server_name crstoreportal.foodconceptsplc.com www.crstoreportal.foodconceptsplc.com;
#     return 301 https://$server_name$request_uri;
# }
